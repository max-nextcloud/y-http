{
  "name": "y-http",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fix": "prettier -w .",
    "lint": "oxlint",
    "test": "oxlint && vitest run && prettier -c ."
  },
  "prettier": "@nextcloud/prettier-config",
  "dependencies": {
    "lib0": "^0.2.105",
    "y-protocols": "^1.0.6",
    "yjs": "^13.6.26"
  },
  "devDependencies": {
    "@nextcloud/prettier-config": "^1.2.0",
    "oxlint": "^0.18.0",
    "typescript": "~5.7.2",
    "vitest": "^3.1.2"
  }
}
